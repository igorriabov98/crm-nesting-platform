import { NestingUploadForm } from '@/components/features/nesting/NestingUploadForm'

export const metadata = { title: 'Новая раскладка — CRM Завода' }

export default function NewNestingProjectPage() {
  return <NestingUploadForm />
}
